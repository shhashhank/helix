/** DI tokens for the notification plumbing (shared by the notifier and the read API). */
export const NOTIFICATION_DISPATCHER = Symbol('NOTIFICATION_DISPATCHER');
export const RECIPIENT_DIRECTORY = Symbol('RECIPIENT_DIRECTORY');
export const IN_APP_INBOX = Symbol('IN_APP_INBOX');
